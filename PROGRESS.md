# PROGRESS.md — 进度与分步计划

> 跨会话的「记忆」。开工先读这里找「下一步」，收尾回来勾选 + 记日志。
> 规则见 `AGENTS.md`。需求真源是 `DESIGN.md`。

---

## 当前状态 / 下一步

**已完成**：脚手架 + Phase 0~9（后端 API、Supabase、本地/线上部署、前端 shell/登录/导航/作业列表/新建/明细/编辑、视觉还原与模块清单补全）全部交付；后续补强已将作业确认状态改为 6 个 bool 勾选项（订舱确认/放舱确认/放箱确认/舱单确认/海关放行/开船确认），Supabase 已直接迁移并插入两条样例，登录页密码框支持眼睛切换明文/密文。
**下一步 → 按用户反馈做增量优化；push main 后由 Vercel 自动部署。**

---

## 分步计划（一次做一个 Phase，达成「验收」才勾选）

> 拆分原则：每个 Phase 自成一个可测试的小闭环，做完能跑、能验、能停。

- [x] **Phase 0 — 仓库初始化**
  - 目标：建 `frontend/`(Next.js+TS+Tailwind) + `backend/`(FastAPI) + `vercel.json` + `requirements.txt` 骨架；后端建 **venv**（`backend/.venv`）、建 `backend/tests/`；写 `.gitignore`（含 `.venv`、`node_modules`、`*.db`、`.env`）。
  - 验收：前端 `npm run dev` 出默认页 ✓（200，默认页）；后端在已激活 venv 内 `uvicorn` 起得来，`GET /api/health` 返回 200 ✓（`{"status":"ok"}`，pytest 一条用例绿）。

- [x] **Phase 1 — 数据模型 + 本地库**
  - 目标：`backend/models.py` 建 `job` 表（字段全照 DESIGN.md §4）；`db.py` 引擎/会话，先用本地 SQLite。
  - 验收：建表脚本跑通 ✓（起服务自动建表，`freight.db` 的 `job` 表 101 列，正好对齐 §4：基本 43+服务标志 18+托单 32+状态节点 6+审计 2）；insert 一条 select 出来 ✓（`tests/test_models.py` 绿，含默认值落库校验）。

- [x] **Phase 2 — 后端 API（CRUD + 登录 + job_no）**
  - 目标：`/api/login`、`POST/GET/GET{id}/PUT/DELETE /api/jobs`、job_no 自动生成、列表过滤+分页。
  - 验收：pytest 覆盖每个接口 happy path + 至少：必填缺失 422、查不到 404、登录错密码失败 ✓（`tests/test_jobs.py` 12 例 + 全套 14 绿）；真实 SQLite 端到端冒烟通过（login/create/list 过滤/get/put/delete）。

- [x] **Phase 3 — 接 Supabase**
  - 目标：`DATABASE_URL` 切到 Supabase 连接池串（端口 6543）。
  - 验收：对 Supabase 跑一遍 CRUD 冒烟脚本通过 ✓（`scripts/smoke_supabase.py` 全绿：建表 + 登录正/误 + 建单 201/缺必填 422 + 列表过滤 + 明细/查无 404 + 编辑 + 删除 204 + 删后 404，结束清空测试数据；14 条回归 pytest 仍全绿）。

- [x] **Phase 4 — 前端骨架（shell + 登录 + 导航）**
  - 目标：`layout.tsx`(导航+退出登录)、`login`、`store.ts`(Zustand+localStorage)、`modules.ts`、主页卡片、`unavailable` 占位页。
  - 验收：浏览器自测——登录跳主页 ✓、未登录访问受保护页跳回登录 ✓（`/`、`/jobs` 均跳）、退出登录清标记 ✓（localStorage `freight-auth` 回 loggedIn:false）、未开放卡片跳占位页 ✓、错密码红字提示 ✓、console 无报错 ✓；Vitest `__tests__/modules.test.ts` 3 绿（分组/过滤纯函数）；`npm run build` 干净。

- [x] **Phase 5 — 作业列表（查询 + 删除）**
  - 目标：`jobs/page.tsx` 筛选条 + 表格 + 新建/删除按钮（删除二次确认），`lib/api.ts` 取数。
  - 验收：浏览器自测——列表从后端加载 ✓、筛选生效（URL query，`?consignor=上海`→1 条、`?final_destination=汉堡`→1 条）✓、删除带确认（"确认删除作业 EXP…？"）并重拉（共 3→2 条）✓、console 无报错 ✓；Vitest 7 绿（新增 `__tests__/api.test.ts` 4 例：筛选拼装/空值忽略/DELETE 204/错误抛 detail）；`npm run build` 干净（`/jobs` 静态预渲染）。

- [x] **Phase 6 — 新建 / 明细 / 编辑**
  - 目标：`jobs/new`（基本信息+服务标志+托单信息，必填黄底红星）、`jobs/[id]`（只读+编辑）。
  - 验收：浏览器自测——端到端建一单成功 ✓（建 EXP20260609-003→跳 `/jobs/7`）、明细读出 ✓（节点进度+基本/服务标志/托单三块、字段格式化正确）、编辑保存 ✓（改委托人+状态，只读视图即时反映）、必填校验与错误提示生效 ✓（黄底红星 + 缺项拦截「请填写必填项：操作员、ETD…」不打后端）、明细页删除 ✓（二次确认→回列表）、console 无报错 ✓；Vitest 12 绿（新增 `__tests__/jobForm.test.ts` 5 例：初始状态默认值/编辑回填、必填校验、payload 空串转 null+数字转换）；`npm run build` 干净（`/jobs/[id]` 动态、`/jobs/new` 静态）。

- [x] **Phase 7 — 联调收尾**
  - 目标：全链路冒烟、边界与错误提示打磨。
  - 验收：登录→新建→列表→明细→编辑→删除→退出 全程无阻塞 ✓（HTTP 全链路冒烟：登录错密码 401/正确 200、缺必填 422、建单 201 自动 job_no、明细 200/查无 404、编辑 200 保号改字段、筛选[status/etd 区间/文本]生效、删除 204、删后 404、库回 2 条样例）；开 subagent 对照 DESIGN.md 终审 ✓（A 字段三层[types/fields/models]严格对齐无漏、B 错误路径全 catch 中文提示不阻塞、C CLAUDE.md §4 约定逐条满足；未实现项均为 DESIGN 标注「可选增强」如 `nodes` 置位接口，不阻塞）。

- [x] **Phase 8 — 视觉还原（对 Penpot 调样式）+ 模块清单补全**
  - 前提：Phase 4~7 功能全做完、各页面都已存在（登录/主页/列表/新建/明细）。先把功能做对，最后统一调视觉，功能与视觉分开。
  - 目标 A（模块清单补全）：按 **DESIGN.md 为准**把规划中的模块补进 `lib/modules.ts`（name/group/`enabled:false`），让主页卡片 + 侧边导航铺满、未开放项点击统一跳 `/unavailable`——复用已有机制，**不碰字段/路由/业务逻辑**。模块清单最终也落到 DESIGN.md，保持前后设计一致；不够再增量加。
  - 目标 B（视觉还原）：用 Penpot MCP（`high_level_overview` → 取设计稿配色/间距/布局/组件样式）把各页面样式 + 主页分组排版贴近 Penpot 视觉稿；**只动样式（Tailwind class / 布局），不改字段、路由、交互逻辑**。冲突处仍以 DESIGN.md 为准（Penpot 仅样式参考）。
  - 验收：主页/导航列全规划模块、未开放跳 `/unavailable` ✓；登录/主页/列表/新建/明细 与 Penpot 视觉一致（配色、间距、必填黄底红星等关键样式核对）；回归——既有 Vitest 全绿、浏览器自测关键路径仍通、console 无报错。

- [x] **Phase 9 — 部署上线**
  - 目标：Vercel（前端+Python函数）+ Supabase，环境变量配置。
  - 验收：线上网址 `freight-system-seven.vercel.app` 可访问 ✓、用 `test` 账号登录进入系统 ✓（GitHub 集成自动部署：push main 即上线）。

> 阶段切换点（建议开新对话）：Phase 2→3、Phase 3→4、Phase 6→7、Phase 7→8。

---

## 变更日志（append-only，新的写最上面）

- **2026-06-11（四轮）** — 按用户截图修正作业窗体「动作」菜单与跨页 QA。① `BookingAction` 真实按钮改云海橙色操作态（`accent #ff8c1a`），菜单 hover 同步橙色浅底，避免与普通黑色工具项混淆。② 下拉/轻提示改为按按钮坐标渲染的 `fixed` 浮层，并提高层级，绕开横向工具栏 `overflow-x-auto` 对纵向菜单的裁剪；`JobWindow` 次级工具栏保留单行横向排列。③ 浏览器自动点击检查：登录→主页→作业列表→明细→动作菜单→新建页→子页签占位→未开放页→侧栏未开放入口，全部通过；动作菜单坐标验证在按钮下方、未被框架遮挡，颜色为 `rgb(255, 140, 26)`；console 无错误、network 无失败请求。验证：Vitest 13 绿、ESLint 绿、`npm run build` 绿。
- **2026-06-11（三轮）** — 按用户 5 张截图继续收敛云海 UI（接续未完成 stash，只改前端呈现，不碰字段/路由/后端业务）。① **订舱弹窗统一**：`BookingAction` 完成态由现代大圆角卡片改为云海式方角系统窗（蓝色标题栏、4px 圆角、克制白底勾选图标与按钮），菜单/提示也改小圆角。② **窗体菜单修正**：`JobWindow` 顶级工具栏、主页签、次级工具栏改为单行横向滚动，按钮进一步压缩；次级工具栏补浅橙底和橙色分隔，避免图 4 那种换行后菜单顺序混乱。列表页顶部工具栏同步单行。③ **登录页文案与装饰字段**：标语从「引领物流进入AI智能时代」改为「引领物流进入智能时代」；分支机构代码不再默认 `YH`，分支机构/部门均保持装饰禁用态，真实登录仍只用用户代码/密码。④ **占位 icon 背景**：子页签 `TabPlaceholder` 与 `/unavailable` 模块占位 icon 由浅蓝块改为白底灰色细描边，减少与蓝色面板冲突。验证：Vitest 13 绿、ESLint 绿、`npm run build` 绿、后端 pytest 14 绿；本地后端 `127.0.0.1:8000` + 干净前端 `127.0.0.1:3001` 浏览器冒烟通过（登录→新建页→子页签占位→未开放页→明细动作订舱弹窗，console 无报错），关键断言：登录文案无 AI、branch_code 为空、占位 icon 白底灰色、订舱弹窗圆角 4px。
- **2026-06-11（二轮）** — 界面对齐云海·第二轮（按用户三张截图做高保真还原 + **侧栏补全云海全部模块**，仍只动样式/布局/导航数据，不碰字段/路由/业务逻辑）。① **侧栏补全 17 个模块**：`SIDEBAR_GROUPS`（业务中心/系统两组 7 项）改为云海式平铺 `SIDEBAR_ITEMS`（我的工作台/海运出口/海运进口/空运出口/空运进口/陆运操作/铁路运输/本地业务/业务操作/仓库管理/集装箱管理/PO管理/运价管理/财务管理/客户管理/基础数据/系统设置），`NavIcons.tsx` 补 10 个线性图标；仍仅海运出口真实。② **顶部改云海双条**：蓝色软件栏（云朵 logo，h-12）+ 橙色页签条（模块=深色激活块、右侧灰字当前页名 `crumbLabel`）。③ **主页模块面板**：组名由 6 色改统一主色蓝（删 `GROUP_COLOR`，`groupedModules()` 返回 `{group,items}`）、链接改流式密排、右上装饰 ×；统计分析组照云海改为 作业箱量分析/作业利润分析/委托人利润分析。④ **整箱作业窗体**：新增 `_components/JobWindow.tsx`（蓝标题栏「整箱/海运出口」+右侧作业号/状态 → 顶级工具栏[新建/删除/关闭真实，航线确认…装饰，船东确认等灰显] → 主页签[作业激活/装箱/费用/凭证/代理对账单/附件/物流可视化] → 次级工具栏槽），新建/编辑/明细共用；`JobToolbar` 改云海次级工具栏（view 态以实心「编辑」打头，明细页大标题/编辑/删除/返回列表收进窗体）；`Toolbar.tsx` 装饰按钮改扁平文字（加 `muted` 变体与 `WindowTitleBar`）；列表页套「作业列表/海运出口」标题栏、筛选区改蓝面板。⑤ **表单分块改云海实底蓝面板**（`panel` `#eef2f9`→`#d5e6f8`，删 `panel-head`，增 `panel-line`）：块内粗体标题、基本信息与服务标志并入同一面板（明细服务标志由 chips 改只读勾选网格，与表单同构）；必填项由浅天蓝改**云海米黄**（`required #fdf3e3`）；`JOB_TABS` 对齐云海 9 页签（去 额外费用/利润减少，增 目的港服务）并改盒式页签（激活块与蓝面板连体）。⑥ **登录页改云海居中布局**：左云朵 logo+品牌+标语+橙字「测试库」，右四行表单（分支机构代码/部门/模拟登录/二维码为装饰、不参与登录；密码框浅蓝底）。回归：Vitest 13 绿（modules 测试新增 SIDEBAR_ITEMS 17 项用例）、eslint/build 干净；浏览器自测（本地起后端）登录→主页→列表→新建→明细→编辑→放弃→关闭全链路通、必填米黄/面板蓝实测准确、console 无报错。踩坑：dev 服务与 `npm run build` 共用 `.next` 缓存打架致旧 CSS 一直被服务——删 `.next` 重启 dev 即恢复。UI.md/AGENTS.md §4 已同步。
- **2026-06-11** — 界面对齐「云海」货代系统（按用户截图还原，**只动样式/布局/导航数据，不碰字段/路由/业务逻辑**）。① **新增设计语言文档 `UI.md`**（根目录）：记录设计基调、颜色令牌、布局骨架、各组件样式与「想改什么改哪」速查；AGENTS.md §4 同步引用。② **`globals.css` `@theme` 扩令牌**：加 `topbar`/`topbar-dark`（顶栏蓝渐变）、`accent`/`accent-soft`（云海橙，激活页签/操作链接）、`panel`/`panel-head`（表单分块浅蓝灰面板）；`line` 微调 `#eaedf4`→`#e3e8f1`。③ **`AppShell` 重构布局**：原「侧栏含品牌 + 顶栏面包屑」改为云海式「顶部蓝色软件栏（品牌 + 横排页签 `TOP_TABS`『模块/工作台/最近打开』，模块激活态橙色下划线 + 右侧用户名/退出）+ 其下侧栏」；侧栏激活项加左 3px 主色竖条。④ **主页 `page.tsx`** 改平面密排：去大圆角阴影卡片，组名彩色粗体 + 下边线，已开放项改橙色链接（呼应云海高亮操作链接）。⑤ **表单/明细** `JobForm`/`JobDetailClient` 分块改云海面板：浅蓝标题条（主色竖条）+ 浅蓝灰面板体（`bg-panel`），字段由「标签上输入下」改「标签左输入右」密排，控件方角。⑥ **工具栏/列表** 去 `rounded-2xl`/`rounded-lg` → 平面 `border` + `rounded`(4px)。回归：Vitest 12 绿、eslint 干净；preview 浏览器自测三屏（模块导航/新建表单/作业列表）截图核对与云海一致、console 无报错（列表 500 仅因本地未起后端，非样式问题）。
- **2026-06-10** — 增量功能：编辑表单工具栏「动作 ▾」下拉（演示用），随后**按反馈简化**为纯提示。新组件 `app/jobs/_components/BookingAction.tsx`：下拉 5 项（订舱/退舱/锁定作业/作废/转工作号），点「订舱」弹窗走发送动画（转圈「正在向船公司发送订舱 EDI…」约 1.3s）→ 完成态「订舱指令已发送 / 已向船公司发出订舱请求。」（勾选图标 +「知道了」关闭）；其余弹「暂不可用」轻提示（1.8s 自动消失，撑场面便于录 demo）。**纯演示、零副作用**：发送动画只为大气，完成态**只告知指令已送达、不声称订舱成功**；不生成回执号、不回填表单、不接真实 EDI、不动后端、不改字段/路由（初版曾做「回执→回填 so_no+勾订舱确认」，按反馈砍掉，连带删 `fields.ts` 的 `makeBookingReceipt`/`BookingReceipt` 及其单测）。**工具栏三态**：抽出共用组件 `app/jobs/_components/JobToolbar.tsx`（`mode: new|edit|view`），`JobForm`(new/edit) 与 `JobDetailClient`(view) 共用——保存/放弃在新建/编辑可点、查看灰显；「动作 ▾」在查看/编辑可点、新建灰显（订舱是对已落库作业单发起的动作）。即查看明细时也显示这条工具栏（保存/放弃灰显占位），可直接点「动作→订舱」。回归：Vitest 12 绿、eslint 绿；浏览器 QA：三态按钮可点性正确（new=保存放弃可点/动作灰、view=保存放弃灰/动作可点、edit=全可点）、查看态点「订舱」弹发送动画→「订舱指令已发送」、console 无报错；DESIGN.md §13.1 同步。备注：沙箱 Linux 跑 Vitest 需补 rolldown 的 linux-arm64 native binding（仅本地 node_modules，未入 package.json）。
- **2026-06-09** — 交付后补强：① 根据云海截图把旧的时间戳“状态节点”改为 6 个无顺序依赖的 bool 确认状态（`booking_confirmed`/`space_released`/`container_released`/`manifest_confirmed`/`customs_released`/`sailing_confirmed`），同步 `DESIGN.md`、`backend/models.py`、`frontend/types/job.ts`、`fields.ts`、表单/明细/列表。② Supabase 真实库已直接迁移：旧 `*_at` 节点列删除，新 bool 列存在，并插入/更新样例 `EXP20260609-201`、`EXP20260609-202`；一次性迁移 SQL/脚本不入仓库，避免后续误跑。③ 新建/编辑与明细页将“作业确认状态”作为独立 card 放在保存工具栏下方、基本信息上方；作业列表新增 6 个 checkbox 列。④ 登录页密码框新增眼睛按钮，支持明文/密文切换。⑤ 收尾检查：pytest 14 绿、Vitest 12 绿、lint 绿、build 绿、`git diff --check` 绿；浏览器 QA 验证密码切换、确认状态 card 位置、列表 6 列、console 无报错；冗余迁移脚本/临时 QA 脚本未入仓库。
- **2026-06-09** — Phase 9 完成（部署上线）。方式 A（GitHub 集成）：Vercel 导入 `Kaiku-AI/freight-system`（先给 Vercel App 授权该组织才在列表里出现），根目录 `./` 由仓库根 `vercel.json` 的 `builds` 同时管前端 Next + 后端 Python，配 3 个环境变量 `DATABASE_URL`(Supabase 6543 连接池串)/`TEST_USER`/`TEST_PASSWORD`。线上网址 `freight-system-seven.vercel.app`，`test/test123` 登录通。关键踩坑：① 首次部署登录 500 `ModuleNotFoundError: No module named 'db'`——后端全用裸导入（`from db/models/routers`），本地在 `backend/` 目录起 uvicorn 能解析，但 serverless 入口是 `backend/index.py` 而 Python 搜索根是仓库根，裸导入全废；修法在 `index.py` 顶部 `sys.path.insert(0, 本文件目录)`，本地与线上导入一致（commit `72b39af`，从仓库根按文件路径加载 index.py 验证 + pytest 14 绿）。② Vercel 导入页 Deploy 按钮卡禁用态——`FastAPI` beta 预设/页面状态 bug，硬刷新重进解决（预设值被 `vercel.json` 接管，无关实际构建）。③ 改环境变量不会自动重部署，需 Deployments→Redeploy 或推新 commit。自此 push main 即自动上线。**本期脊梁（FCL 作业单 CRUD + 极简登录）全部交付完毕。**
- **2026-06-09** — Phase 8 再补强（按用户第二轮反馈，6 点细化，仍只动样式/导航/交互呈现，不碰字段/路由/业务逻辑）：① 左上角 logo 文字 `云`→`运`（侧栏 + 登录页）。② 侧栏图标弃用 emoji，新增 `components/NavIcons.tsx`（7 个线性 SVG：layout-grid/ship/anchor/map-pin/credit-card/database/settings，1.8px 描边、currentColor，激活态主色），还原 Penpot 左栏图标风格。③ 整箱页签改**受控组件**：未实现页签不再跳 `/unavailable`，而在本卡片内显示「暂未开放」占位（`JobTabs`+`TabPlaceholder`，`type=button` 不触发表单提交），新建/明细共用；`JobForm`/`JobDetailClient` 持 tab 状态、抽 `FieldGrid` 去重。④ 顶栏加面包屑「海运出口 / <当前模块>」（首段链 `/`），作为任意模块页回模块导航的入口（侧栏「海运出口」亦可）；`crumbLabel(pathname)` 派生当前模块名。⑤ 模块页加 Penpot 顶部工具栏：`_components/Toolbar.tsx` 的 `ToolbarGhost`（`disabled` 灰显、不可点）——表单页 保存/放弃（真实）+ 订舱模板/相关操作/动作/数据交换/通知/系统功能（装饰）；列表页 新建整箱（真实）+ 复制/删除/热搜索/动作/打印/数据交换/数据分析/导出/系统功能（装饰）。⑥ 同步 CLAUDE.md §4 + DESIGN.md §7.1。回归：浏览器自测——logo=运、侧栏 7 图标（激活 `#5b5bd6`）、面包屑「海运出口/新建整箱」、表单工具栏 8 项（订舱模板等 `disabled`）、点「集装箱」页签停在 `/jobs/new` 且内联占位、必填黄底仍准、console 无报错；Vitest 12 绿、build 干净。
- **2026-06-09** — Phase 8 补强（按用户反馈修信息架构 + 列全）。问题：① 进了具体模块没有回模块导航页的入口、模块页像「全屏」；② 侧栏/主页功能/表单页签都没按 Penpot 列全。改法（仍只动样式/导航数据，不碰字段/路由/业务逻辑）：① **侧栏改为 Penpot 顶层导航**（`SIDEBAR_GROUPS`：业务中心=工作台/海运出口/海运进口/本地业务/财务管理 + 系统=基础数据/系统设置），仅「海运出口」→ 真实主页 `/`（模块导航）、其余 → `/unavailable`；「海运出口」在 `/` 与 `/jobs*` 下高亮——故任意模块页都能由常驻侧栏点「海运出口」回到模块导航（解决回不去/全屏）。② **主页功能网格列全 Penpot 56 项**（6 组 委托单13/操作20/本地业务9/费用11/文档1/统计分析2，从 Penpot 文本精确提取），仅 新建整箱/作业列表 真实，其余点击 `/unavailable`；`GROUP_COLOR` 扩到 6 组。③ **整箱作业加页签条**（`JOB_TABS` 10 个：托单信息/其它信息/船东舱单/货物/PO Item/集装箱/额外费用/MB/L/HB/L/利润减少），仅托单信息为当前内容、其余 → `/unavailable`，新建/明细共用 `JobTabs.tsx`（Section/SectionShell 加 `header` 支持）。`modules.ts` 重构为三块单一源（侧栏导航/功能网格/页签）。回归：浏览器自测主页 56 项 + 侧栏分组 + 表单 10 页签（DOM 实测 托单信息=主色 span 带下划线、其余 `/unavailable` 链接）全过、console 无报错；Vitest 12 绿（modules 测试分组改 6 组）、build 干净。DESIGN.md §7.1 与 CLAUDE.md §4 同步重写。
- **2026-06-09** — Phase 8 完成（视觉还原 + 模块清单补全）。**只动样式/导航数据，不改字段、路由、业务逻辑**（守住 Goal B 边界）。① **接 Penpot MCP**：连上设计稿（`整箱·海运出口` 文件 11 个 board），导出登录/模块导航/作业列表/整箱作业四张视觉稿，提取调色板。② **视觉令牌单一源**：`app/globals.css` 用 Tailwind v4 `@theme` 定义语义色（`brand #5b5bd6`/`brand-soft #eef0ff`/`ink #161823`/`body #3d4150`/`muted #8a91a6`/`line #eaedf4`/`canvas #f4f6fb`/`required #fffbf1`/`star #f04a6e` 等，均取自 Penpot），全站弃用裸 `zinc-/amber-`；出运状态胶囊配色集中到 `fields.ts` 的 `statusBadgeClass`（草稿=蓝/出运中=绿/已关闭=紫灰）。③ **五页面还原**：登录改双栏（左品牌渐变 + 三勾卖点，右白卡「欢迎回来」）；主页改 Penpot 密集分组样式（白卡 + 色点分组 + 链接网格，已做项主色、未开放跳 `/unavailable`）；列表/表单/明细统一卡片化（主色竖条段标题、必填黄底红星 `#fffbf1`、状态胶囊、主色按钮/链接）；AppShell 侧栏加品牌区 + 业务中心分组 + 用户页脚 + 激活态主色高亮。④ **模块清单补全（Goal A）**：`lib/modules.ts` 补到 11 项（委托单/操作/本地业务/费用 4 组，含 MB/L·HB/L·集装箱·AMS·ISF·仓储·报关·费用·发票），加 `GROUP_COLOR` 派生主页色点，`groupedModules()` 返回 `{group,color,items}`；清单同步落回 DESIGN.md §7.1。⑤ 回归：浏览器自测（preview）登录/主页/列表/新建/明细五页面逐一截图核对、console 无报错、必填底色实测 `rgb(255,251,241)` 准确；Vitest 12 绿、`npm run build` 干净。关键决策：品牌名仍用 DESIGN 的「海运出口订舱」而非 Penpot「云枢货代」（内容以文档为准，Penpot 仅样式参考）；登录页只保留账号/密码/登录，未加 Penpot 的记住我/社交登录（非功能项，避免死 UI）。备注：Penpot 账号级 MCP Server 已 Enabled，但本套工具走「Penpot MCP 插件」连接——需在编辑器内打开设计文件并启动插件才通。下一步 Phase 9（部署上线，需用户提供 Vercel/Supabase 凭据，建议开新对话）。
- **2026-06-09** — Phase 7 完成（联调收尾）。**纯验收 Phase，无代码改动**——审查确认实现已完整、约定无违背。① 全链路 HTTP 冒烟（对运行中的 Supabase 后端）：登录错密码 401 / 正确 200 → 缺必填 422 → 建单 201（自动 job_no `EXP20260609-003`）→ 明细 200 / 查无 404 → 编辑 PUT 200（job_no 保留、consignor/status/mbl_payment/pol 更新）→ 筛选（status=draft 1 条 / etd 区间 2 条 / 文本 ilike）生效 → 删除 204 → 删后 404 → 库恢复 2 条样例，全绿。② subagent 对照 DESIGN.md 终审通过：A 字段三层（`types/job.ts` / `fields.ts` / `models.py`）与 §4 一一对应、类型与必填一致，无漏缺；B 错误路径（401/422/404 + 网络异常）前端全 catch 且中文提示不阻塞；C CLAUDE.md §4 约定逐条满足（状态边界 / 模块单一源 / 取数走 api.ts / Suspense / 字段元数据驱动 / pool_pre_ping / params await / 唯一 auth gate / 不超范围）。未实现项均为 DESIGN 明确标注的「可选增强」（`POST /jobs/{id}/nodes/{node}` 节点置位接口未做，故节点进度目前只读；列表「提单」列取 `bl_confirmed_at`，待视觉阶段再校映射），不构成阻塞。③ 回归：pytest 14 绿、Vitest 12 绿、`npm run build` 干净。备注：本环境 preview_* 浏览器工具未接入，故全链路以 HTTP/API 层冒烟替代浏览器自测（前端逻辑已由 Vitest 12 例 + build 类型检查覆盖，UI 接线与 Phase 4~6 浏览器自测一致未变）。下一步 Phase 8（视觉还原 + 模块清单补全，属另一主题，建议开新对话）。
- **2026-06-09** — 计划微调 + 小重构。① **模块清单补全并进 Phase 8**：规划中的模块（MB/L、HB/L、集装箱、进仓、报关、费用…）以 `enabled:false` 录进 `lib/modules.ts`，让主页/导航铺满、未开放跳 `/unavailable`——复用现有机制，不改字段/路由/逻辑；**清单以 DESIGN.md 为准**（前后设计一致，不够再增量加），最终落回 DESIGN.md。与视觉还原同期做（共用设计稿一次打开）。② **重命名 `JobsClient.tsx`→`JobListClient.tsx`**（函数同步改名），与 `JobDetailClient.tsx` 区分更清晰（列表 vs 明细）；改 `app/jobs/page.tsx` 引用。回归 build 干净 + Vitest 12 绿。
- **2026-06-09** — Phase 6 完成（新建/明细/编辑）。新增 `app/jobs/_components/fields.ts`（**字段元数据单一数据源**：`BASIC_FIELDS`/`CONSIGNMENT_FIELDS`/`SERVICE_FLAGS`/`NODE_FIELDS` + 选项常量 + 共享纯函数 `initialFormState`/`missingRequired`/`buildJobPayload` + `statusLabel`/`fmtDate`/`fmtDateTime`，驱动新建表单/编辑表单/只读明细三处渲染）、`app/jobs/_components/JobForm.tsx`（新建/编辑共用，必填黄底红星，提交前必填校验，新建成功跳明细、编辑成功回调刷新）、`app/jobs/_components/JobDetailClient.tsx`（只读明细：节点进度+三块字段+服务标志 chips，含编辑/删除）、`app/jobs/new/page.tsx`（服务端壳）、`app/jobs/[id]/page.tsx`（服务端壳 `await params`，非法 id→notFound）。重构 `JobsClient.tsx` 复用 fields.ts 的 `STATUS_OPTIONS/statusLabel/fmtDate`（去重）。回归：`__tests__/jobForm.test.ts` 5 例（全套 Vitest 12 绿）、pytest 14 绿、build 干净、浏览器自测端到端全过。关键决策/踩坑：① **表单字段元数据驱动**：~75 字段不手写 75 个 input，由 `FieldDef[]`（name/label/type/required/options/full）渲染，新增/改字段只动 fields.ts；纯函数（校验/payload 构建）抽到 fields.ts 与 React 解耦，便于单测。② **Next 16 动态路由 `params` 是 Promise**，`[id]/page.tsx` 须 `await params`（已读 node_modules/next/dist/docs 确认）。③ **修了 Supabase 连接池死连接 bug**：自测建单首发 500「SSL SYSCALL error: EOF detected」——pgbouncer 回收空闲连接后池里留死连接，复用即崩；`db.py` 加 `pool_pre_ping=True`（非 SQLite）取用前探活自动重连，serverless/长闲置必备。④ **datetime-local** 控件需 `YYYY-MM-DDTHH:mm`，回填时 `slice(0,16)`。⑤ 新建预填后端默认值（业务性质=整柜订舱/出运类型=整箱/状态=draft/订舱日期=今天）让用户可见可改。⑥ 自测在真库建的测试单（id 7）已用「明细页删除」路径删除清理，库恢复 2 条样例（EXP20260609-001/-002）。⑦ Claude Preview 合成事件仍不触发 React 受控 onChange/onClick——用原生 value setter + dispatch input/change、`.click()`、`requestSubmit()`、stub confirm 验证（同 Phase 5）。下一步 Phase 7（联调收尾，建议开新对话）。
- **2026-06-09** — Phase 5 完成（作业列表）。新增 `types/job.ts`（全量镜像后端 `JobBase`+系统字段，date/datetime 为 ISO 字符串，作字段唯一对齐源，Phase 6 直接复用）、`lib/api.ts`（统一 `request` helper 抛后端 detail、204 返 undefined；`getJobs`[筛选→URLSearchParams 空值忽略]/`getJob`/`createJob`/`updateJob`/`deleteJob`，全程原生 fetch 不用 SWR）、`app/jobs/page.tsx`（Suspense 包 `JobsClient`）+ `app/jobs/_components/JobsClient.tsx`（筛选条+表格+分页+删除）。回归：`__tests__/api.test.ts` 4 例（全套 Vitest 7 绿）。关键决策/踩坑：① **筛选放 URL query**（DESIGN §3.4）：表单 `name` → `onSearch` 收成 URLSearchParams → `router.push`（offset 归零）；`useSearchParams` 变化触发 `load()` 重拉；分页 `offset` 也在 URL。② Next 16 要求用 `useSearchParams` 的客户端组件包 `<Suspense>`，否则 build 报错——故 page.tsx 作服务端壳套 Suspense。③ 列「提单」DESIGN 未明确映射哪个字段（与 拖车=trucking/报关=customs_declare 并列），暂取「提单确认节点」`bl_confirmed_at` 是否完成渲染 ✓/—，Phase 6/视觉阶段可再校。④ 筛选 UI 取列对齐的常用项（作业号/委托人/船名/MBL/起运港/目的地/状态/ETD 区间）；后端另支持 voyage/booking_agent，UI 暂未放（保持筛选条简洁，需要再加）。⑤ **Claude Preview 自测踩坑**：`preview_click` 的合成点击不触发 React 受控 `onSubmit`/`onClick`，改用 `requestSubmit()`/原生 `.click()`（并 stub `window.confirm`）才验到提交与删除——处理器本身无误，纯工具限制。⑥ 浏览器自测在 Supabase 真库建了 3 条中文样例单（删 1 留 2：EXP20260609-001/-002），留给 Phase 6 明细/编辑自测用，非生产数据。下一步 Phase 6（新建/明细/编辑）。
- **2026-06-09** — 计划调整：新增 **Phase 8「视觉还原（对 Penpot 调样式）」**，原部署上线顺延为 Phase 9。决策背景：Penpot 全程定位为「样式参考」（DESIGN §0/§7.1，冲突以文档为准），原计划无专门视觉还原步骤，前端骨架先用功能型 Tailwind 样式（不刻意还原 Penpot）；与用户确认后定为「功能全做完，最后统一对 Penpot 调样式」——功能与视觉分开，视觉 Phase 只动样式不改字段/路由/交互。
- **2026-06-09** — Phase 4 完成（前端骨架）。新增 `lib/store.ts`（Zustand+persist 存登录标记，name `freight-auth`）、`lib/modules.ts`（模块清单唯一数据源 + `enabledModules`/`groupedModules` 派生）、`components/AppShell.tsx`（全局 shell：客户端 auth gate + 侧栏导航[只列 enabled] + 顶栏退出登录）、`app/login`、`app/page.tsx`（主页按 group 渲染卡片，未开放跳 `/unavailable`）、`app/unavailable`；改 `app/layout.tsx`（中文 lang/metadata，套 AppShell）、`app/globals.css`（去 dark-mode 媒体查询，统一浅色）。`next.config.ts` 加 dev rewrites：`/api/*` → `BACKEND_ORIGIN`(默认 `http://localhost:8000`)，仅 development 生效（线上走 vercel.json）。回归：Vitest 装好 + `__tests__/modules.test.ts` 3 绿（`vitest.config.ts` 配 `@/` 别名，加 `npm test` 脚本）。关键决策/踩坑：① 登录态纯客户端（Zustand+localStorage，无 token），故 auth gate 也在客户端 AppShell；persist 水合前用 `mounted` 标志门控，`mounted` 前 return null 避免 SSR 首帧闪烁与误跳转。② login 是「shell 外」——AppShell 对 `PUBLIC_PATHS=['/login']` 裸渲染、不套导航；已登录访问 /login 自动跳主页。③ 浏览器自测用 Claude Preview（Chrome 扩展未连），新增 `.claude/launch.json`（preview_start 用，`npm --prefix frontend run dev`）。④ 本机 8000 端口此次空闲，后端正常起在 8000（后端连 Supabase 启动需 ~8s）。下一步 Phase 5（作业列表）。
- **2026-06-09** — Phase 3 完成。接 Supabase：`backend/.env`（已 gitignore）写入 `DATABASE_URL`=Transaction pooler 连接池串（端口 6543，`postgresql+psycopg2://`）；`requirements.txt` 加 `psycopg2-binary`+`python-dotenv`；`db.py` 顶部 `load_dotenv()` 使本地 uvicorn/脚本能读 `.env`（线上由平台注入，无需 `.env`）。新增 `backend/scripts/smoke_supabase.py` 冒烟脚本（建表+CRUD 全链路+自清理，长期保留）。关键决策/踩坑：① 用 Transaction pooler(6543) 而非 Direct(5432)，psycopg2 同步驱动默认不开服务端 prepared statement，与 pgbouncer 事务模式兼容；② `TestClient(app)` 非 `with` 用法**不触发 lifespan**，故脚本显式调 `create_db_and_tables()` 建表；③ 建单返回 **201**、删除返回 **204**（标准 REST，先前 Phase 2 已如此，勿误期望 200）。回归 pytest 仍 14 绿。下一步 Phase 4（前端，建议开新对话）。
- **2026-06-09** — Phase 2 完成。`models.py` 重构为 SQLModel 五件套（`JobBase` 业务字段真源 → `Job` 表 + `JobCreate`/`JobRead`/`JobList`/`LoginRequest`）。`routers/jobs.py`：5 个 CRUD 接口（POST 建单自动生成 job_no、GET 列表支持 §5 全部筛选[文本 ilike 模糊/status 精确/etd 区间]+limit/offset 分页+total、GET{id}/PUT[整单覆盖业务字段保号刷 updated_at]/DELETE，缺失 422/查无 404）。`index.py` 挂 router + `/api/login`（环境变量 `TEST_USER/TEST_PASSWORD` 默认 test/test123，错密码 401）。`job_no` 规则 `EXP+YYYYMMDD+-三位流水`，按当天最大序号递增。回归 `tests/test_jobs.py` 12 例（全套 14 绿）。备注：job_no 生成在高并发下有竞态但有 unique 兜底，本期单机够用；本地与前端跨域留 Phase 4 用 Next rewrite 代理解决。下一步 Phase 3。
- **2026-06-09** — Phase 1 完成。`models.py` 建 `Job` 表（101 字段，分基本/服务标志/托单/状态节点/审计五块，逐一对照 DESIGN §4；必填字段非 Optional，`business_type`/`shipment_type`/`status`/`booking_date` 给默认，重量体积用 float，审计 `created_at/updated_at` default_factory）；`db.py` 按 `DATABASE_URL` 选引擎（默认 `sqlite:///./freight.db`，SQLite 放开线程检查），`get_session` 依赖 + `create_db_and_tables`；`index.py` lifespan 启动建表。回归测试 `tests/test_models.py`（独立内存 SQLite，insert+select+默认值校验）。下一步 Phase 2。
- **2026-06-09** — Phase 0 完成。前端 `create-next-app`（Next 16 + TS + Tailwind + App Router，无 src 目录）；后端 FastAPI 骨架 `backend/index.py`（`GET /api/health`），建 `.venv` 并装 fastapi/uvicorn/sqlmodel/httpx/pytest；测试 `backend/tests/test_health.py` 绿（`backend/pytest.ini` 设 `pythonpath=.` 使可 import index）；补 `vercel.json` 骨架（builds+routes，待 Phase 8 实测）。**注意：本机 8000 端口被 Docker 占用，本地起后端请用 8001 或先停 Docker。** 下一步 Phase 1。
- **2026-06-09** — 补充三条约定：后端用 venv、测试文件长期保留为回归套件（`backend/tests/`）、全项目代码整洁不冗余。
- **2026-06-09** — 建立工作流脚手架：CLAUDE.md（约定+协议）、PROGRESS.md（分步计划）。下一步 Phase 0。
